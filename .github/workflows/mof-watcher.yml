name: M.Of. Watcher Daily

on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:

jobs:
  mof-watch:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install anthropic requests beautifulsoup4 lxml

      - name: Generate prompt
        run: |
          cd managed-agents
          python mof_watcher.py

      - name: Invoke Claude Managed Agent
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          cd managed-agents
          python <<'PY'
          import os
          import datetime
          from pathlib import Path
          from anthropic import Anthropic

          today = datetime.date.today().isoformat()
          prompt = Path(f"output/mof-watch/prompt-{today}.txt").read_text(encoding="utf-8")

          client = Anthropic(api_key=os.environ["ANTHROPIC_API_KEY"])
          message = client.messages.create(
              model="claude-opus-4-8",
              max_tokens=8192,
              messages=[{"role": "user", "content": prompt}],
          )
          report_text = "".join(block.text for block in message.content if hasattr(block, "text"))
          Path(f"output/mof-watch/{today}.md").write_text(report_text, encoding="utf-8")
          print(f"Report saved: output/mof-watch/{today}.md")
          PY

      - name: Commit reports
        run: |
          cd managed-agents
          git config user.email "mof-watcher-bot@local"
          git config user.name "MOF Watcher Bot"
          git add output/
          git diff --quiet --staged || git commit -m "mof-watch: report $(date +%Y-%m-%d)"
          git push || echo "Push failed"
